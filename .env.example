PUBLIC_API_URL=http://localhost:8787
PUBLIC_PRIVY_APP_ID=cmrvyylgv00wn0djxlqhssxe7

# Observability (production only — leave blank locally so dev traffic/errors are ignored)
# Cloudflare Web Analytics beacon token: CF dashboard -> Web Analytics -> add site -> copy token
PUBLIC_CF_BEACON_TOKEN=
# Sentry (client-side JS errors): Sentry -> project (astro) -> DSN. Safe to expose.
PUBLIC_SENTRY_DSN=
PUBLIC_SENTRY_ENVIRONMENT=production
