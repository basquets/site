---
interface Props {
  class?: string;
  rule?: boolean;
  id?: string;
}
const { class: className = "", rule = false, id } = Astro.props;
---
{rule && <hr class="h-0.5 border-0 bg-divider m-0" />}
<section id={id} class:list={["py-[calc(2.5*28px)]", className]}>
  <slot />
</section>
